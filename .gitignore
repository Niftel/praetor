# Secrets / keys
keys
*.pem
inventory/gitea_ed25519
.env
web/.env.local

# Compiled service binaries (rebuild via `make build`)
/main
/api
/consumer
/controller
/temp_controller
/executor
/host-runner
/ingestion
/migrator
/scheduler
/packbuilder
/execpack
/praetor-host-runner-linux
/build/praetor-host-runner
/build/linux/
# Self-contained Ansible runtime bundle — a large build artifact produced by
# `make ansible-runtime`, not tracked in git.
/build/runtime/
/bin/

# Local TLS certs for Traefik — machine-specific (generated by mkcert against the
# host's local CA); never committed. Regenerate per deployments/traefik/dynamic/tls.yml.
deployments/traefik/certs/

# Logs
*.log
executor_logs.txt

# Frontend
node_modules/
web/dist/
*.zip

# Local backups (recoverable from git history)
web_backup_v1/
web_config_backup/

# OS / editor cruft
.DS_Store
