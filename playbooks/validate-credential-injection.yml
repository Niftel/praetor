---
- name: Validate credential injection without disclosing it
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Verify the Machine credential reached Ansible
      ansible.builtin.assert:
        that:
          - lookup('ansible.builtin.env', 'ANSIBLE_PASSWORD') | length >= 32
      no_log: true
